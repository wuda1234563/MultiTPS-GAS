# MultiTPS 项目开发约束

## 0. 项目定位

MultiTPS 是一个以 C++ 为主、蓝图为辅、基于 Unreal Engine Gameplay Ability System（GAS）的多人第三人称射击游戏 Demo。

本项目的首要目标不是单纯快速堆功能，而是在可运行的前提下，持续学习和实践正式游戏项目中的：

- Git 分支、提交、评审与集成规范；
- C++、蓝图、GAS 的职责划分；
- 面向多人游戏的客户端/服务器权威模型；
- 接口、委托、组件、数据资产和事件驱动解耦；
- 数据驱动、可配置、可扩展的系统设计；
- 功能文档、配置清单和可复用制作流程。

所有后续实现都应优先考虑：职责是否清晰、网络行为是否正确、是否容易测试、是否容易由策划配置、是否能避免 Hard Coding，以及未来是否容易扩展。

## 1. 协作方式与教学要求

1. 每次开始实现前，先说明目标、假设、影响范围和验证方式。
2. 如果需求存在多种合理解释，必须先指出差异；会改变架构或工作量的歧义不能静默决定。
3. 每完成一个功能或模块，都要同步编写或更新项目文档，记录实现方式、使用方法、配置项、网络注意事项、常见错误和后续扩展点。
4. 讲解实现时，不只说明“怎么做”，还要说明“为什么这样做”、替代方案以及在正式项目中的取舍。
5. 每次修改结束后，给出验证结果；如果无法在当前环境验证，必须明确说明未验证项。
6. 修改应保持小而聚焦。不得顺手重构无关代码、改变既有风格或引入未要求的抽象。

## 2. Git 与代码集成规范

### 2.1 仓库与主分支

- `main`（或团队约定的稳定主分支）禁止直接开发和直接提交。
- 每个功能、修复、重构或文档变更都必须从最新主分支创建独立分支。
- 分支合并前应完成自测、差异检查和提交历史检查；多人项目中应通过 Pull Request/Merge Request 评审。
- 当前项目若尚未初始化 Git，必须先由项目所有者确认仓库初始化、`.gitignore`、远程地址和默认分支策略，再进行首次提交。

### 2.2 分支命名

推荐使用以下命名：

```text
feature/<short-description>
fix/<short-description>
refactor/<short-description>
docs/<short-description>
chore/<short-description>
```

示例：

```text
feature/gas-character-attributes
fix/replicated-projectile-hit
docs/enemy-configuration
```

分支名称使用小写英文和短横线，表达一个清晰的工作目标，不把多个无关功能混在同一分支中。

### 2.3 提交规范

提交信息采用 Conventional Commits 风格：

```text
<type>(<scope>): <imperative summary>
```

常用 `type`：

- `feat`：新增功能；
- `fix`：修复问题；
- `refactor`：不改变行为的重构；
- `docs`：文档变更；
- `test`：测试或验证代码；
- `build`：构建、插件或工程配置；
- `chore`：其他维护性变更。

要求：

- 一个提交只表达一个完整、可解释的变更；
- 提交标题使用英文祈使句，简短明确，不使用“修改一下”“完成开发”等无信息量描述；
- 不提交编译产物、缓存、临时文件、编辑器用户配置、敏感信息或本机专属路径；
- 大型功能应拆分为可独立审查的提交，例如先提交接口/数据结构，再提交运行逻辑，最后提交配置和文档；
- 如果一个提交无法说明“为什么需要它”，说明提交粒度或变更范围可能不合适。

示例：

```text
feat(gas): add replicated character attribute set
fix(weapon): prevent client-only hit confirmation
docs(enemy): document montage and ability setup
```

### 2.4 标准开发流程

每次功能开发遵循以下流程：

1. 同步主分支并创建任务分支；
2. 明确需求、验收标准和网络边界；
3. 实现最小可用改动；
4. 在编辑器、PIE 多客户端或自动化测试中验证；
5. 检查 `git diff`、`git status`，确认没有无关文件；
6. 提交一个或多个逻辑清晰的 Commit；
7. 推送分支并发起 Pull Request/Merge Request；
8. 根据评审意见修订，保持历史可读；
9. 评审通过后合并主分支，并删除已完成的任务分支。

每次实际提交前，必须向项目作者说明：使用的分支、提交命令、提交信息、验证命令以及这样做的原因。当前仓库未初始化 Git 时，不得假装已经完成上述流程。

### 2.5 Unreal Engine 专项 Git 注意事项

- 默认忽略 `Binaries/`、`DerivedDataCache/`、`Intermediate/`、`Saved/` 等生成目录；
- 只提交项目需要的源码、配置、内容资产和插件源码；
- 对二进制资产（`.uasset`、`.umap`）的修改必须在提交说明中写明用途和依赖配置；
- 修改 `.uproject`、插件启用状态、输入映射、Gameplay Tags 或网络相关配置时，应单独说明影响范围；
- 发生资产重命名或移动时，必须确认引用未断裂，避免只提交文件系统层面的移动；
- 合并前检查编辑器是否产生了不应提交的自动修改。

## 3. C++ 与蓝图职责划分

### 3.1 总原则

采用“C++ 建立稳定能力和边界，蓝图完成组合、表现和易变配置”的方式。

C++ 优先承载：

- 核心规则、状态机、可复用算法和高频逻辑；
- 网络复制、服务器权威校验、预测与回滚相关逻辑；
- GAS 的 AttributeSet、GameplayEffect 基础结构、Ability 基类、ASC 集成；
- 接口、组件、数据访问层、异步流程和可测试逻辑；
- 对蓝图暴露的最小稳定 API。

蓝图优先承载：

- GameplayAbility 的具体技能编排（当节点表达比 C++ 更清晰时）；
- 动画、特效、音效、镜头、Timeline、Lerp 等表现逻辑；
- 基于 DataAsset/DataTable 的资源绑定和实例化配置；
- 低频、强表现、经常由设计调整的流程。

### 3.2 新增 C++ 类型的流程

助手不得直接在文件系统中擅自创建新的 Unreal C++ 类文件。

当功能需要新类型时，必须先告诉项目作者：

1. 应创建的父类（例如 `ACharacter`、`UActorComponent`、`UAttributeSet`、`UGameplayAbility`、`UPrimaryDataAsset`、`UInterface` 等）；
2. 推荐的类名、模块和目录；
3. 该类的职责、网络归属和生命周期；
4. 需要暴露给蓝图的属性、函数、事件以及原因；
5. 创建后需要由助手实现的接口和验证步骤。

项目作者在 Unreal Editor 中创建类后，助手才能编辑和实现该类。已有类的实现、头文件、配置和文档可以在需求范围内修改。

### 3.3 蓝图事件宏的使用

- `BlueprintCallable`：C++ 提供可复用的动作或业务 API，蓝图负责调用和编排；
- `BlueprintPure`：无副作用的查询函数；
- `BlueprintImplementableEvent`：C++ 定义扩展点，具体表现完全由蓝图实现；
- `BlueprintNativeEvent`：提供 C++ 默认实现，同时允许蓝图覆盖；
- `BlueprintReadOnly` 优先于 `BlueprintReadWrite`，除非确实需要蓝图写入；
- 不为了“方便蓝图”而暴露内部状态、裸指针或可破坏不变量的函数。

例如召唤技能可以由 C++ 提供 `BlueprintCallable` 的召唤函数，蓝图 Ability 负责选择时机和表现；宝箱开启动画可由 C++ 提供 `BlueprintNativeEvent` 或 `BlueprintImplementableEvent`，蓝图使用 Timeline 与 Lerp 完成表现。

## 4. GAS 与多人网络规范

- 服务器是关键游戏状态的最终权威；客户端输入可以预测表现，但不能自行确认伤害、资源扣除、生成和胜负结果；
- 明确区分 Server、Owning Client、其他客户端的执行范围；
- Gameplay Ability、Gameplay Effect、Gameplay Tag、AttributeSet 的职责不得混淆；
- 持久状态优先使用 Attribute、Tag、组件或可复制属性表达，避免散落的布尔变量；
- 伤害、治疗、消耗、冷却、控制效果优先通过 GameplayEffect 和 Tag 体系表达；
- 通过 AbilityTask、事件、委托和接口解耦 Ability 与角色、武器、UI、动画；
- 重要流程必须考虑预测 Key、复制顺序、重复触发、断线和客户端不可信输入；
- 避免在 Tick 中轮询可以由事件、委托、Tag 变化或 AbilityTask 驱动的逻辑；
- GameplayTag 字符串、资产引用和配置项不得散落硬编码，应集中管理并记录配置来源。

## 5. 解耦、数据驱动与可扩展性

- 优先通过接口表达能力，通过委托/事件表达通知，通过组件表达可组合行为；
- Actor 不应直接依赖具体 UI、具体敌人子类或具体武器实现；
- 使用 DataAsset/DataTable 保存设计数据，将“规则”与“数值/资源配置”分离；
- 运行时状态与配置数据分离，避免修改 DataAsset 作为临时运行状态；
- 依赖具体类之前，先检查是否可以依赖接口、抽象基类、能力组件或数据访问接口；
- 所有跨系统引用都要说明拥有者、生命周期和网络边界；
- 禁止为单次需求引入不必要的通用框架、反射层或多级抽象。

## 6. 功能文档要求

每完成一个功能或模块，必须在 `Docs/` 下创建或更新对应文档。推荐命名：

```text
Docs/<ModuleName>.md
```

每份模块文档至少包含：

1. 功能目标与范围；
2. 运行流程和关键类/组件关系；
3. C++ 与蓝图职责划分；
4. GAS 配置：Ability、Effect、Tag、Attribute、AbilityTask；
5. 网络行为：服务器、拥有客户端、模拟客户端分别执行什么；
6. 资产和子蓝图创建步骤；
7. 必须配置的 Montage、动画、特效、音效、输入、DataAsset、GameplayTag 等；
8. 批量创建或新增变体时的检查清单；
9. 常见错误、调试方法和验证结果；
10. 已知限制、性能风险和后续扩展建议。

文档必须与代码/资产变更放在同一个任务分支中，并在提交信息或 PR 描述中互相引用。项目完成后，再将模块文档汇总为总项目总结文档，内容包括架构图、模块依赖、网络模型、GAS 设计、工具链、Git 流程、问题复盘和可作为求职项目亮点的技术深度。

## 7. 技术深度候选方向

重要功能实现前，可以从以下方向选择深入程度；是否加入项目由项目作者结合学习目标和工期决定：

- GAS 网络预测、客户端预测窗口、预测 Key 与服务端校验；
- 可复制 GameplayCue、命中反馈、伤害归因和 Gameplay Event 链；
- 武器/投射物的服务器权威、客户端表现预测与命中回溯；
- 基于 GameplayTag 的状态机和数据驱动技能系统；
- Replication Graph、对象相关性和多人场景性能分析；
- Animation Montage、Root Motion、Motion Warping 与网络同步；
- DataAsset 驱动的武器、敌人、技能和掉落配置；
- 模块化 Gameplay Feature、插件化系统和可选功能加载；
- 自动化测试、多人 PIE 测试、网络模拟和性能分析；
- Unreal Insights、Stat Net、Network Profiler 与 GAS 调试工具链。

每个候选方向都应说明：学习收益、实现成本、对当前 Demo 的实际价值、验证方式，以及是否值得作为求职项目亮点。

## 8. 变更完成标准

一个功能只有同时满足以下条件，才视为完成：

- 代码或蓝图实现符合职责边界；
- 已完成必要的单机、多人 PIE 或自动化验证；
- 网络权威和复制行为已说明；
- 必要的资产、Tag、Effect、Ability 和子蓝图配置已记录；
- 文档已更新；
- Git 差异干净、提交粒度清晰，且没有直接提交主分支。

## 9. 重要功能与 GAS 模块的实现模板

重要功能、核心模块和 GAS 架构能力不能只留下最终代码。每次实现都必须同时产出一份可复习、可复用的“实现笔记”。这份笔记既是项目文档，也是学习材料和后续与策划沟通的依据。

### 9.1 适用范围

以下内容必须使用本模板：

- AttributeSet、AbilitySystemComponent、GameplayAbility、GameplayEffect、GameplayCue、GameplayTag 等 GAS 核心模块；
- 角色、武器、投射物、伤害、治疗、死亡、复活、装备、背包、敌人 AI 等核心玩法模块；
- 任何涉及复制、RPC、客户端预测、服务器校验、多人 PIE 或性能的重要功能；
- 后续可能被批量创建、继承、配置或复用的系统。

### 9.2 实现笔记模板

推荐在 `Docs/Notes/` 下按以下结构记录：

```text
# <功能或模块名称> 实现笔记

## 1. 学习目标
- 这项功能要解决什么问题
- 本次要掌握哪些 UE/GAS/网络概念
- 完成后能够迁移到哪些其他功能

## 2. 需求与验收标准
- 玩家或策划能看到什么结果
- 单机、主机、客户端、模拟客户端分别应如何表现
- 哪些情况算失败

## 3. 核心概念预习
- 相关 UE/GAS 类的职责
- 相关网络概念的通俗解释
- 本功能为什么选择这种架构

## 4. 类型与职责设计
- 需要哪些已有类
- 如果必须新增 C++ 类，应由项目作者在 UE 编辑器中创建的父类和类名
- C++、蓝图、数据资产各自负责什么
- 接口、委托、组件、GameplayTag、DataAsset 如何解耦

## 5. 分步实现流程
1. 准备输入、Tag、资产和配置
2. 创建或修改 C++ 类型
3. 在 C++ 中实现稳定规则和可复用 API
4. 在蓝图中完成组合、表现或易变逻辑
5. 配置 Ability、Effect、Cue、Montage、动画和资源
6. 接入角色、武器、UI 或其他系统
7. 加入网络复制、RPC、预测或服务器校验
8. 完成调试与验证

## 6. GAS 运行时流程
- 输入如何触发 Ability
- Ability 如何检查 Tag、Cost、Cooldown 和权限
- Effect 如何修改 Attribute
- Gameplay Event、Cue、委托或接口如何通知其他系统
- Ability 结束、取消、失败和重复触发如何处理

## 7. 网络执行流程
- 谁发起请求
- 哪些逻辑只在服务器执行
- 哪些逻辑可以在拥有客户端预测
- 哪些状态需要复制
- RPC 的方向、可靠性和参数校验
- 模拟客户端看到什么
- 延迟、丢包、重复请求或客户端作弊时会怎样

## 8. 蓝图连线概要
- 以文字或简图描述主要节点顺序
- 标出输入、分支、AbilityTask、事件、Timeline、Montage 和结束节点
- 说明哪些节点是表现，哪些节点代表实际游戏结果

## 9. 配置清单
- Gameplay Tags
- Attribute、GameplayEffect、GameplayAbility、GameplayCue
- Input、Montage、Animation、Sound、Niagara
- DataAsset、DataTable、子蓝图和默认值
- 复制相关设置、网络更新频率和必要的组件配置

## 10. 验证与调试
- 单机验证
- 两个或多个客户端 PIE 验证
- 服务器日志、客户端日志和关键断点
- GAS 调试器、GameplayTag 日志、网络统计或 Unreal Insights
- 已知问题和未覆盖场景

## 11. 复盘与迁移
- 本次实现中最重要的设计决定
- 哪些地方可以抽象为基类、组件、接口或数据资产
- 下一个类似功能可以复用哪些步骤
- 当前实现的性能、可维护性和扩展风险

## 12. 可复用提示词
- 用于重新实现本功能的简短提示词
- 用于创建同类变体的提示词
- 用于排查常见问题的提示词
```

### 9.3 “模版方法”要求

对于可复用流程，应总结成接近模版方法的固定步骤，而不是只描述某一次实现。例如“新增一个 GAS 技能”至少要固定检查：

1. 输入和 GameplayTag 是否已定义；
2. Ability 的授予者、拥有者和激活权限是否明确；
3. Cost、Cooldown、阻断 Tag 和取消规则是否配置；
4. 服务器执行、客户端预测和表现触发点是否区分；
5. 伤害/治疗等实际结果是否由服务器确认；
6. Montage、Cue、Effect、音效和特效是否配置；
7. Ability 结束、取消、死亡和重复激活是否能正确清理；
8. 多客户端 PIE 是否验证了所有角色看到的结果；
9. 文档和批量配置清单是否已更新。

模板不是为了制造形式主义，而是为了让同一套思考可以迁移到冲刺、射击、装填、投掷、召唤和控制技能等不同功能。

## 10. 网络知识教学基线

项目作者已经掌握 C++、UE 和 GAS 的基本使用，但不默认掌握多人网络编程。凡是涉及网络的实现，都必须遵循“先讲概念，再做实现”的教学顺序。

### 10.1 不得默认的知识

不得默认项目作者已经理解以下内容：

- 客户端、服务器、拥有客户端和模拟客户端的区别；
- Actor、Component、属性和 GameplayEffect 的复制；
- Server RPC、Client RPC、NetMulticast 的用途和边界；
- Reliable/Unreliable 的取舍；
- Authority、Role、Ownership、NetMode 和相关生命周期；
- 客户端预测、预测 Key、确认、拒绝、回滚和重复执行；
- RepNotify、复制条件、复制优先级、网络更新频率和相关性；
- 服务器权威、防作弊和客户端不可信输入；
- 延迟、丢包、乱序、重复消息以及多人 PIE 测试方法。

### 10.2 每次涉及网络时的讲解顺序

实现前，先用当前功能的具体例子解释：

1. 谁拥有状态，谁有权修改状态；
2. 客户端发送的是什么请求；
3. 服务器验证什么并产生什么结果；
4. 哪些结果复制给谁；
5. 客户端可以提前表现什么，最终结果如何被服务器确认；
6. 如果客户端作弊、重复发送或网络延迟，会出现什么问题；
7. 如何在 PIE 多客户端中观察和验证上述流程。

之后再介绍 Unreal 的类、宏、属性说明符、RPC 或 GAS API，并把每个 API 对应回上述流程。涉及术语时，第一次出现必须给出简短定义，不直接使用只对熟悉网络编程者有意义的缩写。

### 10.3 网络实现的最低验收标准

任何多人功能至少要记录并验证：

- 服务器是否是关键结果的最终权威；
- 客户端是否能通过伪造参数直接改变结果；
- 属性、Tag、Ability 状态和表现是否按预期同步；
- 主客户端、其他客户端和服务器视角是否分别正确；
- 高延迟或重复输入下是否会重复造成伤害、消耗资源或生成对象；
- 断线、死亡、取消和 Ability 结束时是否清理了预测状态与委托；
- 验证结果、测试拓扑和已知限制是否写入模块文档。

网络部分的目标不是一开始就背诵所有 API，而是逐步建立“谁决定结果、谁看到结果、结果如何同步”的正确心智模型。
